id: baseline-evidence-pack
name: Baseline Evidence Pack
version: 1.0.0
description: Generic evidence
required_artifacts:
  - report.json
rules:
  - id: has_metrics
    metric: metrics
    operator: exists
    weight: 100
